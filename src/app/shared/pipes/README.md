# pipes

## Proposito
Pipes reutilizables para formateo de datos en templates.

## Responsabilidad unica
Pipes reutilizables para formateo de datos en templates.

## Contiene
- Transformaciones como `hora-formato`, `rol-label`, `fecha-corta`.

## No contiene
- Llamadas HTTP o dependencias de estado global.

## Referencia
- Basado en `skeleton.md` para `src/app/shared/pipes/`.
